services:
  api:
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: development
    volumes:
      - ./src:/app/src
      - /app/node_modules
    command: ["node", "--watch", "src/server.js"]